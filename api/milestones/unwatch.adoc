To stop watching an milestone send a POST request specifying the milestone id in the url

include::../generated/milestones-unwatch-cmd.adoc[]

The HTTP response is a 200 OK with an empty body response
