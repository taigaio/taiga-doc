To get a project send a GET request specifying the project id in the url

include::../generated/projects-get-cmd.adoc[]

The HTTP response is a 200 OK and the response body is a JSON link:#object-project-detail[project detail object]
